enum cameraActions {
    cameraUp,
    cameraDown,
    cameraForward,
    cameraBackward,
    cameraLeft,
    cameraRight,
    CAMERA_ACTION_NUM_ITEMS
};

enum windowActions {
    wireFrameToggle,
    mouseCatchToggle,
    WINDOW_ACTION_NUM_ITEMS
};
